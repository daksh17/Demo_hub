apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Full stack with Kafka (matches docker-compose.yml). Requires base already applied.
resources:
  - ../../zookeeper.yaml
  - ../../kafka.yaml

patches:
  - path: backend-kafka-patch.yaml
